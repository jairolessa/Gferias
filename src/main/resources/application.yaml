server:
  port: 8081

spring:
  application:
    name: Gferias

  datasource:
    url: jdbc:h2:mem:test2db
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: false

  rabbitmq:
    host: jaragua.lmq.cloudamqp.com
    port: 5671
    username: hddykhdy
    password: plJM7Tp8ins6MNMPmTQ-yyLMSGEQZ_sJ
    virtual-host: hddykhdy
    ssl:
        enabled: true
